@model FashionHubWeb.Models.Category

@{
    ViewData["Title"] = "Edit Category: " + Model.CategoryName;
}

<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="card card-outline card-warning shadow-sm">
            <div class="card-header">
                <h3 class="card-title font-weight-bold">
                    <i class="fas fa-edit text-warning mr-2"></i> Edit Category
                </h3>
            </div>
            <form asp-action="Edit" method="post">
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="alert alert-danger py-2"></div>
                    <input type="hidden" asp-for="CategoryId" />

                    <div class="form-group mb-3">
                        <label class="font-weight-bold text-muted">Category ID</label>
                        <input class="form-control font-monospace bg-light" value="@Model.CategoryId" readonly />
                    </div>

                    <div class="form-group mb-0">
                        <label asp-for="CategoryName" class="font-weight-bold">Category Name</label>
                        <input asp-for="CategoryName" class="form-control" required />
                        <span asp-validation-for="CategoryName" class="text-danger small"></span>
                    </div>
                </div>

                <div class="card-footer bg-light d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left mr-1"></i> Back
                    </a>
                    <button type="submit" class="btn btn-warning font-weight-bold text-white px-4">
                        <i class="fas fa-save mr-1"></i> Update Category
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
